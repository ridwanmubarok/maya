version: "3.8"

services:
  bot:
    build: .
    container_name: maya_bot
    restart: always
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    dns:
      - 8.8.8.8
      - 1.1.1.1
